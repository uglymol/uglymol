{
  "name": "gemmimol",
  "version": "0.7.2",
  "description": "Gemmi-backed macromolecular viewer for crystallographers",
  "keywords": [
    "crystallography",
    "biochemistry",
    "webgl",
    "coot",
    "protein",
    "structure"
  ],
  "homepage": "https://uglymol.github.io",
  "repository": {
    "type": "git",
    "url": "https://github.com/uglymol/uglymol.git"
  },
  "license": "MIT",
  "main": "gemmimol.js",
  "module": "src/all.ts",
  "files": [
    "gemmimol.js",
    "gemmimol.min.js",
    "src",
    "vendor/wasm"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm test",
    "bundle": "rollup -c",
    "coverage": "rollup -c --sourcemap && jest --testMatch='**/test/*.js' --coverage --collectCoverageFrom='gemmimol.js'",
    "ci": "npm run download-data && npm test",
    "download-data": "node perf/util.js download-data",
    "lint": "eslint src test perf",
    "prepack": "npm run build",
    "serve": "serve -l 7890",
    "start": "npm run watch & npm run serve",
    "test": "npm run lint && tsc --noEmit src/*.ts && rollup -c && jest --testMatch='**/test/*.js'",
    "version": "npm run build && git add -A gemmimol.js gemmimol.min.js",
    "watch": "rollup -c --watch"
  },
  "prettier": {
    "trailingComma": "es5",
    "tabWidth": 2,
    "semi": true,
    "singleQuote": true
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@rollup/plugin-sucrase": "^5.1.0",
    "@rollup/plugin-terser": "^1.0.0",
    "@types/node": "^25.5.0",
    "benchmark": "^2.1.0",
    "eslint": "^10.0.3",
    "jest": "^30.3.0",
    "rollup": "^4.59.0",
    "serve": "^14.2.6",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.57.0"
  }
}
